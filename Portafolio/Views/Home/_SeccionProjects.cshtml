@model IEnumerable<ProjectDTO>

@if (Model is not null)
{
    <div class="text-center" style="padding-top: 6rem; font-family: 'SF Pro Display'">
        <h1>My recent works</h1>
        <p>Here you can see my recent projects</p>
    </div>

    <partial name = "_ListProyects" model ="Model"></partial>
}